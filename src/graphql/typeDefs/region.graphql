type Region {
    id: Int!,
    name: String!,

    geolocation: Geolocation!
    cities: [City]
}

input Input {
    name: String,
}

type Query {
    region(id: Int!): Region
    regions: [Region]
}

type Mutation{
    createRegion(input: Input): Region
}
